package com.deliverytech.delivery.entity;

// Enum é uma lista fechada de opções.
// Só existem esses 4 tipos de usuário no sistema.
public enum UserRole {
    ADMIN,
    CLIENTE,
    RESTAURANTE,
    ENTREGADOR
}